/**
 * Reusable UI primitives.
 * Export shared components from here for consistent imports: import { X } from "@/components/ui"
 */

// Add exports as components are created, e.g.:
// export { Button } from "./Button";
